# homebrew-tap

Homebrew tap for my own tools.

```sh
brew install jay-snyder/tap/<tool>
```

Casks in this repository are generated and pushed by
[GoReleaser](https://goreleaser.com) when a tool is released — edit the tool's
`.goreleaser.yaml` rather than the generated files here.
